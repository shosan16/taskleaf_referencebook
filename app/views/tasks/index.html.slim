h1 タスク一覧
= link_to  "新規登録",new_task_path, class: 'btn btn-primary'

.mb-3
table.table.table-hover
  thead.thead-default
    tr
      th 名前
      th 登録日時
  tbody
    - @tasks.each do |task|
      tr
        td = link_to task.name, task_path(task), class: 'nav-link'
        td = task.created_at
